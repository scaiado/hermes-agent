# Hermes ↔ Fuli Integration TODO

## Environment boundary (final)

- Fuli development / quality gates run in `/Users/caiado/repos/Fuli-Memory-Core/.venv-clean`
- Hermes integration tests run in the Hermes venv at `/Users/caiado/.hermes/hermes-agent/.venv`
- Runtime smoke tests (real embedder, direct Fuli, shadow) run in the Hermes venv
- Fuli dev extras (`pytest-asyncio`, `mypy`, `types-PyYAML`) are intentionally absent from the Hermes venv
- The earlier 106 failures in the Hermes venv were caused by running Fuli's development test suite in a runtime-only environment, not by source regressions

## Direct Fuli provider
- [x] Refactor `plugins/memory/fuli/__init__.py` to lazy initialization
- [x] Replace global async bridge with per-provider instance bridge
- [x] Add bounded per-call timeout to async bridge
- [x] Add safe shutdown (storage close, loop stop, thread join)
- [x] Verify `utils.atomic_json_write` exists or replace with safe write
- [x] Register Fuli in `hermes_cli/memory_providers.py`
- [x] Add tests for discovery, manifest, lazy init, CRUD, reinforce, diagnostics
- [x] Add isolated smoke test with temporary Hermes home
- [x] Add real-embedding smoke test with temporary Hermes home and DB

## Shadow provider
- [x] Create `plugins/memory/shadow/` plugin
- [x] Load primary (Honcho) and secondary (Fuli) providers by name
- [x] Preserve Honcho tool schemas and dispatch
- [x] Mirror writes to Fuli best-effort
- [x] Sample and compare reads after primary result
- [x] Add SQLite evidence store for comparisons
- [x] Add CLI/report commands
- [x] Add `hermes shadow preflight` command
- [x] Add tests proving safety invariants

## Documentation and discipline
- [x] Update `progress.md` with environment boundary and pilot status
- [x] Run lint/type checks/tests in the appropriate environment
- [x] Commit in small logical commits
- [x] Verify push targets before pushing
- [x] Provide exact safe commands for enabling local shadow pilot

## Environment alignment
- [x] Create clean Fuli venv and confirm 187-passing baseline
- [x] Identify Hermes venv regression cause (missing dev extras)
- [x] Document environment boundary: Fuli dev gates only in `.venv-clean`
- [x] Hermes venv remains runtime/integration-only (no Fuli dev extras installed)

## Fork / branch
- [x] Fork `NousResearch/hermes-agent` to `scaiado/hermes-agent`
- [x] Push integration branch `feat/fuli-shadow-memory`
- [x] Verify commits are preserved on remote

## Pilot
- [x] User go/no-go: granted
- [x] Create dedicated `shadow-pilot` profile (did not modify default `~/.hermes/config.yaml`)
- [x] Run `hermes shadow preflight` in the dedicated profile (passed static checks)
- [x] Add dynamic primary probe to the pilot script
- [x] Confirm Honcho primary endpoint `http://100.77.121.91:8000` is reachable and writes successfully
- [x] Re-run `hermes shadow preflight` and primary probe after endpoint is restored
- [ ] Launch the 4–8 hour conservative mirrored-write pilot
- [ ] Collect exact evidence and update `progress.md` / `TODO.txt`
- [ ] Do not activate normal user profile
- [ ] Do not enable `compare_reads` until the pilot report is reviewed
